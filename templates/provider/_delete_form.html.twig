<form method="post" action="{{ path('provider_delete', {'prov_siren': provider.prov_siren}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ provider.prov_siren) }}">
    <button class="btn">Delete</button>
</form>
