{% extends 'base.html.twig' %}

{% block title %}Group{% endblock %}

{% block body %}
    <h1>Group</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Group_id</th>
                <td>{{ group.groupId }}</td>
            </tr>
            <tr>
                <th>Group_name</th>
                <td>{{ group.groupName }}</td>
            </tr>
            <tr>
                <th>Group_descr</th>
                <td>{{ group.groupDescr }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('group_index') }}">back to list</a>

    <a href="{{ path('group_edit', {'group_id': group.group_id}) }}">edit</a>

    {{ include('group/_delete_form.html.twig') }}
{% endblock %}
